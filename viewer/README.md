# Process Maps

An Express web app for browsing and viewing process map assets (maps, images) from the ckassets library.

Serves a visual interface for navigating the Candlekeep campaign's asset library via a local server.
